import { styled } from "@mui/material";

export const BoxCenter = styled("div")(({ theme }) => ({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    width: "48px",
    height: "43px",
  }));